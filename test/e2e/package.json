{
  "name": "paylessforai-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "matrix": "playwright test --config=matrix.config.ts",
    "matrix:real": "MATRIX_REAL=1 playwright test --config=matrix.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
